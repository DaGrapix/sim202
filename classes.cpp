#include "classes.hpp"

void box::append_particle(particle& part){
    if (p_particle==nullptr){
        p_particle = &part;
        return;
    }
    if ()
    double center[8][3];
    for (int i = 1; i <= 2; i++){
        for (int j = 1; i <= 4; j++){
            if ((i==1) && (j==1)){

            }
        }
    }
    
}